# Core dependencies
PyQt6>=6.4.0
PyQt6-Qt6>=6.4.0
PyQt6-sip>=13.4.0
loguru>=0.7.0
pyyaml>=6.0
psutil>=5.9.0  # System resource monitoring
requests>=2.31.0  # HTTP requests for PTZ control

# GStreamer Python bindings
# Note: PyGObject requires system-level GStreamer installation
# On Ubuntu/Debian: sudo apt-get install python3-gi python3-gi-cairo gir1.2-gstreamer-1.0
# On Windows: Install via MSYS2 or GStreamer binaries
PyGObject>=3.42.0

# Optional dependencies for enhanced features
numpy>=1.24.0  # For image processing
opencv-python>=4.8.0  # For computer vision features (optional)
Pillow>=10.0.0  # For image manipulation

# Development dependencies (optional)
pytest>=7.4.0
pytest-qt>=4.2.0
black>=23.0.0
flake8>=6.0.0

# Documentation (optional)
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0